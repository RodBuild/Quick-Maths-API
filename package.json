{
  "name": "math_friendly_api",
  "version": "1.0.0",
  "description": "The API will return math formulas intented to be used on a timed quiz environment.",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "test": "cross-env NODE_ENV=test jest --testTimeout=30000",
    "start": "node server.js",
    "swagger": "node ./swagger/swagger.js"
  },
  "author": "Rodrigo Rodriguez",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.4.2",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.3",
    "swagger-autogen": "^2.23.1",
    "swagger-ui-express": "^4.6.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongodb": "^4.13.0",
    "mongoose": "^6.9.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
